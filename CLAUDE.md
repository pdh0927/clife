# clife

Claude 플랜 사용량을 메뉴바에 보여주는 macOS 앱. Swift + AppKit, 의존성 없음.
공개 레포(MIT)라 커밋 메시지와 주석은 남이 읽는다는 전제로 쓴다.

Execution model: `.claude/rules/execution-model.md`.

## Project Map

| 경로 | 역할 |
|---|---|
| `src/main.swift` | API 클라이언트, 응답 캐시, 상태 아이템, 드롭다운, 알림, 진단 플래그. 대부분이 여기 |
| `src/dog.swift` | 강아지 일러스트(포즈 데이터 + Core Graphics 그리기). UI 로직 없음 |
| `src/widget.swift` | 바탕화면 패널. 드롭다운의 뷰를 **재사용**하고 다시 만들지 않는다 |
| `raycast/claude-usage.py` | Raycast 스크립트 커맨드. 앱과 캐시 파일을 공유 |
| `design/` | 시안(SVG/HTML)과 `implementation/`(빌드된 앱에서 뽑은 실물 렌더) |
| `build.sh` / `setup.sh` | 빌드(swiftc 직접 호출) / 빌드+`/Applications` 설치 |

`src/`에 파일을 추가하면 **`build.sh`의 `swiftc` 줄에도 추가해야 한다.** Xcode
프로젝트가 아니라 파일 목록이 명시적이다.

## Rules & Preferences

- **의존성을 추가하지 않는다.** AppKit + 표준 라이브러리로 끝낸다. Raycast
  스크립트도 파이썬 표준 라이브러리만 쓴다(`pip` 없음, `jq` 없음).
- **사용자에게 보이는 문자열은 한국어, 코드 주석은 영어.** 기존 파일의 언어를
  바꾸지 않는다.
- **`assert` 대신 `precondition`.** `build.sh`가 `-O`로 빌드해서 `assert`는
  통째로 제거된다. 셀프테스트가 조용히 아무것도 검사하지 않게 된다.
- 로직을 고치면 `--selftest`에 회귀 케이스를 남긴다. 프레임워크는 쓰지 않는다.
- **같은 것을 두 번 그리지 않는다.** 위젯과 드롭다운은 한 뷰를 공유한다. 두 벌이
  되는 순간 같은 숫자를 다르게 말하기 시작한다.
- README는 한국어가 기본이고 `README.en.md`가 대응본이다. **한쪽만 고치지 않는다.**
- 동작을 바꿨으면 README의 해당 문단도 같이 고친다. 이 프로젝트의 README는 기능
  목록이 아니라 *왜 그렇게 했는지*의 기록이라, 코드와 어긋나면 그냥 틀린 글이 된다.

## Capability Boundaries

**쓸 수 있는 것**
- `GET https://api.anthropic.com/api/oauth/usage` — 토큰은 키체인
  `Claude Code-credentials`에서 `/usr/bin/security`로 **읽기만**. 갱신은 Claude
  Code 몫이고, 우리가 쓰면 refresh token을 둘 다 잃는다.
- `Clife --selftest` 로직 검증 / `--probe` 실제 요청 1회 / `--dogsheet <path>`
  일러스트·드롭다운 레이아웃을 PNG로 출력.

**할 수 없는 것 — "안 된다"고 잘못 판단하기 쉬운 것들**
- **WidgetKit 익스텐션은 못 쓴다.** 애드혹 서명이라 로드되지 않는다. 바탕화면
  위젯은 데스크톱 레벨 `NSPanel`로 구현되어 있다(이게 정답이지 차선이 아니다).
- **실행 중인 `NSMenu`는 캡처할 수 없다.** 캡처하려는 순간 닫힌다. 레이아웃
  확인은 `--dogsheet`의 오프스크린 렌더로 한다.
- **합성 `CGEvent`로는 숨은 메뉴바가 내려오지 않는다.** 풀스크린에서의 호버 동작은
  자동 검증이 불가능하니 사용자에게 확인을 요청한다.
- **Higgsfield 크레딧 0 · free 플랜.** 이미지 생성은 유료 트라이얼 결제가 필요하다
  — 사용자 결정 사항이지 내가 진행할 일이 아니다.

## Lessons Learned

**레이트 리밋이 이 프로젝트의 중심 제약이다.** 몇 초 안에 5번 부르면 `429`에
`Retry-After: 296`. 예산이 마르면 **16초 간격 두 번**으로도 막힌다. 폴링을 촘촘하게
하고 싶어지면 먼저 이걸 떠올린다. 디버깅한다고 연달아 두드리면 **진단하려던 증상을
직접 재생산하게 된다** — 실제로 그렇게 30분을 날렸다.

- **`Retry-After: 0`을 주면서 계속 거절한다.** 짧은 창은 풀렸는데 긴 한도가 남은
  상태다. 서버 숫자는 하한선이지 상한선이 아니다. 곧이곧대로 믿으면 30초마다 벽에
  들이받는다.
- **`resets_at`은 호출마다 ±1초 흔들린다**("지금 + 남은 시간"으로 계산되어 초 경계를
  넘나든다). 정확히 비교하면 "새 창이 시작됨"으로 오판해 이미 지난 임계값 알림이
  매번 다시 울린다. 허용오차로 비교한다.
- **`inFlight` 같은 중복 방지 플래그에는 반드시 타임아웃을 건다.** 콜백이 안 오면
  이후 모든 갱신이 첫 줄에서 조용히 리턴하고, UI는 아무 말도 안 한 채 몇 시간 묵은
  숫자를 들고 있는다.
- **`security` 서브프로세스는 무한 대기할 수 있다.** 키체인이 잠겼거나 승인
  다이얼로그가 풀스크린 창 **뒤에** 떠서 안 보이면 영영 안 돌아온다.
- `URLSession.shared`를 쓰지 않는다. 디스크 HTTP 캐시와 전역 쿠키 저장소를 끌고
  다녀서, 캐시된 응답이 최신값처럼 반환될 수 있다.

**측정과 검증에서 물린 것들**

- **`/usr/bin/sample`로 프로파일링하면 그 비용이 대상 프로세스에 계상된다.** 이걸로
  "유휴 2.1% CPU"라는 존재하지 않는 문제를 만들어냈다. 순간 CPU는 `top -l`로 본다.
- **`CGWindowList`의 owner 이름은 `CFBundleDisplayName`이다** — 여기선 `Clife`가
  아니라 `Usage Monitor for Claude Code`. 바이너리 이름으로 찾으면 창이 없는 줄 안다.
- **macOS에 `timeout(1)`이 없다.** 백그라운드 로거가 통째로 안 돌았다.
- 오프스크린 뷰 렌더는 `appearance`를 명시하고 `cacheDisplay` **뒤에** 배경을
  합성해야 한다. 안 그러면 "라벨이 안 그려진다"고 오진한다.
- `occlusionState`는 데스크톱 레벨 창에 안 먹는다(가려져도 visible로 보고).

**일러스트 작업 규칙 (비싸게 배움)**

좌표를 눈 감고 조금씩 바꾸면서 매번 렌더해 보는 방식은 **수렴하지 않는다.** 귀·목줄·
주둥이를 한꺼번에 흔들었다가 두 번 연속 더 나빠져서 되돌렸다. 한 번에 **변수 하나만**
바꾸고 `--dogsheet`로 확인한다. 취향 문제는 사용자가 깨어 있을 때 빠른 피드백
루프로 푼다 — 혼자 오래 돌리면 비용만 든다.

`design/`의 SVG는 도형을 원·타원·둥근 선·2차 곡선으로 제한한다. `NSBezierPath`에
1:1 대응해야 시안과 구현이 조용히 갈라지지 않는다.
